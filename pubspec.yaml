name: sky_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: 3.11.3

dependencies:
  flutter:
    sdk: flutter
  
  #Storage
  shared_preferences: 2.5.4

  #State Management
  provider: any

  #Routing
  go_router: ^17.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

flutter:
  uses-material-design: true
